---
wave: 5
depends_on:
  - "01-07"
files_modified:
  - tests/conftest.py
  - tests/test_auth.py
  - tests/test_requests.py
  - tests/test_idor.py
  - tests/test_audit.py
autonomous: true
requirements_addressed:
  - NFR-TEST-1
  - NFR-TEST-2
  - NFR-TEST-3
  - NFR-TEST-4
  - FR-AUTH-1
  - FR-AUTH-4
  - FR-REQ-5
  - FR-AUDIT-1
type: tdd
must_haves:
  goal: "pytest passes with ≥80% coverage on approval engine and validation; IDOR, auth, and audit tests all pass"
  truths:
    - "tests/conftest.py sets up in-memory SQLite test DB (not procureflow.db)"
    - "All tests use httpx AsyncClient or TestClient — no direct DB manipulation except fixtures"
    - "test_idor.py demonstrates that requester A cannot access requester B requests (403 returned)"
    - "test_auth.py covers valid login, wrong password (401), expired token (401), inactive user (403)"
    - "test_audit.py verifies audit rows are created for submit + decision actions"
    - "py -m pytest --cov=app --cov-report=term-missing exits 0 with ≥80% on approval_engine.py"
---

# Plan 01-08: Test Suite (API Integration + IDOR + Audit)

## Objective

Build the full pytest test suite with in-memory DB fixtures. Cover auth, IDOR, request lifecycle, approval decisions, and audit log creation. This is the final gate for Phase 1 done criteria.

## Tasks

<task id="T01" type="execute">
  <title>Create tests/conftest.py — test fixtures and in-memory DB</title>
  <read_first>
    - app/db/base.py — Base, engine, get_db, SessionLocal
    - app/main.py — app instance for TestClient
    - app/db/models.py — all models (needed for create_all)
    - app/core/security.py — get_password_hash for fixture users
    - scripts/seed.py — pattern for creating test users
  </read_first>
  <action>
    Create tests/conftest.py with:

    TEST_DATABASE_URL = "sqlite:///./test.db"
    Use a separate test engine: test_engine = create_engine(TEST_DATABASE_URL, connect_args={"check_same_thread": False})
    TestSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=test_engine)

    Fixture: db_session (function scope):
      Base.metadata.create_all(bind=test_engine)
      session = TestSessionLocal()
      try: yield session
      finally: session.close(); Base.metadata.drop_all(bind=test_engine)

    Fixture: client (function scope, depends on db_session):
      Override get_db dependency to use test session.
      app.dependency_overrides[get_db] = lambda: db_session
      yield TestClient(app)
      app.dependency_overrides.clear()

    Fixture: seed_users(db_session) — creates 4 users:
      alice: role=requester, email=alice@test.com, password hashed from "alice123"
      bob: role=manager, email=bob@test.com, password hashed from "bob123"
      carol: role=finance, email=carol@test.com, password hashed from "carol123"
      admin_user: role=admin, email=admin@test.com, password hashed from "admin123"
      Also create 1 department (IT) and 1 vendor (TestVendor).
      Return dict: {"alice": alice, "bob": bob, "carol": carol, "admin": admin_user}

    Fixture: auth_headers(client, seed_users) — logs in each user and returns headers dict:
      {"alice": {"Authorization": "Bearer <alice_token>"}, "bob": ..., "carol": ..., "admin": ...}

    Import: from fastapi.testclient import TestClient; from sqlalchemy import create_engine; from sqlalchemy.orm import sessionmaker; from app.main import app; from app.db.base import Base, get_db; from app.db.models import User, Department, Vendor; from app.core.security import get_password_hash; import pytest
  </action>
  <acceptance_criteria>
    - tests/conftest.py exists
    - Uses sqlite:///./test.db or in-memory sqlite:///:memory: (NOT procureflow.db)
    - db_session fixture drops and recreates all tables per test function (isolation)
    - client fixture overrides get_db dependency
    - seed_users creates 4 users with correct roles
    - py -m pytest tests/ --collect-only exits 0 (no collection errors)
  </acceptance_criteria>
</task>

<task id="T02" type="execute">
  <title>Create tests/test_auth.py — authentication tests</title>
  <read_first>
    - app/routers/auth.py — endpoint contracts
    - tests/conftest.py — client, seed_users fixtures
    - .planning/REQUIREMENTS.md — FR-AUTH-1 through FR-AUTH-4
  </read_first>
  <action>
    Create tests/test_auth.py with these test cases:

    test_login_success_returns_token(client, seed_users):
      POST /auth/login with email=alice@test.com, password=alice123
      Assert status 200, response has access_token and token_type=="bearer"
      Assert access_token is a non-empty string with 3 dot-separated parts (JWT)

    test_login_wrong_password_returns_401(client, seed_users):
      POST /auth/login with email=alice@test.com, password=wrongpassword
      Assert status 401

    test_login_unknown_email_returns_401(client, seed_users):
      POST /auth/login with email=nobody@test.com, password=anything
      Assert status 401

    test_get_me_with_valid_token(client, seed_users, auth_headers):
      GET /users/me with alice auth header
      Assert status 200, response email == alice@test.com, role == requester

    test_get_me_without_token_returns_401(client):
      GET /users/me with no Authorization header
      Assert status 401

    test_get_me_with_invalid_token_returns_401(client):
      GET /users/me with Authorization: Bearer invalid.token.here
      Assert status 401

    test_inactive_user_returns_403(client, seed_users, db_session):
      Set alice.is_active = False in DB; db_session.commit()
      GET /users/me with alice token
      Assert status 403
  </action>
  <acceptance_criteria>
    - tests/test_auth.py exists and contains ≥6 test functions
    - All tests use TestClient (no direct HTTP)
    - test_inactive_user_returns_403 verifies is_active enforcement
    - py -m pytest tests/test_auth.py -v exits 0 with all tests passing
  </acceptance_criteria>
</task>

<task id="T03" type="execute">
  <title>Create tests/test_requests.py — request lifecycle tests</title>
  <read_first>
    - app/routers/requests.py — endpoint contracts
    - tests/conftest.py — client, seed_users, auth_headers fixtures
    - .planning/REQUIREMENTS.md — FR-REQ-1 through FR-REQ-8
  </read_first>
  <action>
    Create tests/test_requests.py with these test cases:

    Helper: create_request(client, alice_headers, overrides=None) → creates a standard request, returns response JSON

    test_requester_can_create_request(client, seed_users, auth_headers):
      POST /requests/ with valid body as alice
      Assert status 201 (or 200), status=="draft", requester_id==alice.id
      Assert no status, requester_id, assigned_role in request body (anti-mass-assignment check on server)

    test_request_status_defaults_to_draft(client, seed_users, auth_headers):
      Create request as alice; assert response.status == "draft"

    test_submit_request_changes_status(client, seed_users, auth_headers):
      Create request → POST /requests/{id}/submit as alice
      Assert new status is "pending_approval" or "needs_rule" (not "draft")

    test_submit_requires_draft_status(client, seed_users, auth_headers):
      Create and submit request → try to submit again
      Assert 400 (already submitted)

    test_requester_can_see_own_requests(client, seed_users, auth_headers):
      Create request as alice → GET /requests/ as alice
      Assert response list contains the created request

    test_manager_can_see_routed_requests(client, seed_users, auth_headers):
      Create and submit request that routes to manager → GET /requests/ as bob
      Assert request appears in bob's list

    test_requester_cannot_see_others_requests(client, seed_users, auth_headers):
      Create request as alice → GET /requests/ as bob (as requester-equivalent)
      Assert alice's request NOT in bob's list when bob is a different role
      (Test that filter is role-scoped)

    test_invalid_request_body_returns_422(client, seed_users, auth_headers):
      POST /requests/ with quantity=-1 (invalid)
      Assert 422 (validation error)

    test_estimated_cost_must_be_positive(client, seed_users, auth_headers):
      POST /requests/ with estimated_cost=0
      Assert 422

    test_non_requester_cannot_create_request(client, seed_users, auth_headers):
      POST /requests/ as bob (manager role)
      Assert 403
  </action>
  <acceptance_criteria>
    - tests/test_requests.py exists with ≥9 test functions
    - Tests verify status defaults to "draft"
    - Tests verify 422 for invalid body fields
    - Tests verify 403 when wrong role tries to create
    - py -m pytest tests/test_requests.py -v exits 0
  </acceptance_criteria>
</task>

<task id="T04" type="execute">
  <title>Create tests/test_idor.py — IDOR/BOLA protection tests</title>
  <read_first>
    - app/routers/requests.py — _get_request_or_403 logic
    - tests/conftest.py — client, seed_users, auth_headers fixtures
    - .planning/REQUIREMENTS.md — NFR-SEC-2, FR-REQ-5
  </read_first>
  <action>
    Create tests/test_idor.py with these test cases:

    test_requester_cannot_access_other_user_request_detail(client, seed_users, auth_headers, db_session):
      Create a second requester user (carol2 role=requester) in DB fixture
      Create request as alice → GET /requests/{id} as carol2 (different requester)
      Assert 403

    test_requester_can_access_own_request(client, seed_users, auth_headers):
      Create request as alice → GET /requests/{id} as alice
      Assert 200

    test_manager_can_access_assigned_request(client, seed_users, auth_headers):
      Create and submit request that routes to manager → GET /requests/{id} as bob
      Assert 200

    test_manager_cannot_access_finance_request(client, seed_users, auth_headers):
      Create and submit request that routes to finance (high amount) → GET /requests/{id} as bob (manager)
      Assert 403

    test_admin_can_access_any_request(client, seed_users, auth_headers):
      Create request as alice → GET /requests/{id} as admin_user
      Assert 200

    test_idor_uses_403_not_404(client, seed_users, auth_headers):
      Create request as alice → access as a different requester
      Assert status code is 403 (not 404 — must not leak existence)
  </action>
  <acceptance_criteria>
    - tests/test_idor.py exists with ≥5 test functions
    - IDOR violation returns 403, not 404
    - Manager cannot access finance-assigned requests
    - Admin can access any request
    - py -m pytest tests/test_idor.py -v exits 0
  </acceptance_criteria>
</task>

<task id="T05" type="execute">
  <title>Create tests/test_audit.py — audit log creation tests</title>
  <read_first>
    - app/services/audit_service.py — log_action behavior
    - app/routers/requests.py — audit calls in submit
    - app/routers/approvals.py — audit calls in decide
    - tests/conftest.py — client, seed_users, auth_headers, db_session fixtures
    - .planning/REQUIREMENTS.md — FR-AUDIT-1, FR-AUDIT-3
  </read_first>
  <action>
    Create tests/test_audit.py with these test cases:

    test_submit_creates_audit_log(client, seed_users, auth_headers, db_session):
      Create and submit request as alice
      Query AuditLog from DB for request_id
      Assert ≥1 audit row with action containing "submitted" or old_status="draft"

    test_approval_decision_creates_audit_log(client, seed_users, auth_headers, db_session):
      Create → submit → decide as appropriate approver
      Query AuditLog rows for request_id
      Assert row exists with action containing "decision" or new_status in ["approved","rejected","needs_more_info"]

    test_audit_log_is_read_only_via_api(client, seed_users, auth_headers):
      Verify no DELETE /audit/ or PATCH /audit/ endpoints exist
      GET /openapi.json → assert no audit write methods

    test_admin_can_read_audit_for_request(client, seed_users, auth_headers):
      Create and submit request → GET /audit/requests/{id} as admin
      Assert 200 and non-empty list

    test_non_admin_cannot_read_audit(client, seed_users, auth_headers):
      Create request as alice → GET /audit/requests/{id} as alice
      Assert 403
  </action>
  <acceptance_criteria>
    - tests/test_audit.py exists with ≥4 test functions
    - Tests verify audit rows in DB after submit and approve
    - Tests verify non-admin gets 403 on audit endpoint
    - py -m pytest tests/test_audit.py -v exits 0
  </acceptance_criteria>
</task>

<task id="T06" type="execute">
  <title>Final coverage gate — verify ≥80% on approval engine</title>
  <read_first>
    - tests/test_approval_engine.py — existing unit tests
    - tests/ — all test files to run together
  </read_first>
  <action>
    Run the full test suite with coverage:
    py -m pytest --cov=app --cov-report=term-missing --cov-fail-under=80 -v

    If coverage on app/services/approval_engine.py is below 80%, add additional test cases to tests/test_approval_engine.py covering the missing branches (check --cov-report output for uncovered lines).

    Common gaps to check:
    - boundary conditions (exactly at min_amount, exactly at max_amount)
    - rule with both category and amount filters
    - needs_more_info decision transition
    - validate_transition with all terminal state combos

    Do NOT modify implementation to artificially inflate coverage. Fix tests only.
  </action>
  <acceptance_criteria>
    - py -m pytest --cov=app --cov-report=term-missing exits 0
    - app/services/approval_engine.py shows ≥80% coverage in report
    - app/services/audit_service.py shows ≥80% coverage in report
    - Total test count ≥ 30 test functions across all test files
    - Zero test failures
  </acceptance_criteria>
</task>

## Verification

```powershell
# Full test suite with coverage
py -m pytest --cov=app --cov-report=term-missing -v

# Specific coverage check on business logic
py -m pytest tests/test_approval_engine.py tests/test_requests.py tests/test_idor.py tests/test_auth.py tests/test_audit.py --cov=app/services --cov-report=term-missing

# Check no test.db left behind (cleanup in conftest)
ls test.db 2>$null && echo "WARNING: test.db not cleaned up" || echo "test.db cleaned up OK"
```

## Phase 1 Done Criteria Checklist

```
[ ] py -m uvicorn app.main:app starts without error
[ ] py -m alembic upgrade head creates all 7 tables
[ ] py scripts/seed.py creates 4 users + 3 departments + 3 rules + 2 vendors
[ ] POST /auth/login returns token for valid credentials
[ ] POST /auth/login returns 401 for wrong password
[ ] Protected endpoints return 401 for missing/invalid tokens
[ ] POST /requests/ creates draft request (requester only)
[ ] POST /requests/{id}/submit routes to approver role or needs_rule
[ ] IDOR: requester A cannot access requester B request (403 returned)
[ ] POST /approvals/{id}/decide changes status correctly
[ ] Every status change creates an AuditLog row
[ ] GET /audit/requests/{id} returns audit trail (admin only)
[ ] py -m pytest passes — zero failures
[ ] py -m pytest --cov=app shows ≥80% on approval_engine.py
[ ] No .env file committed (.env.example only)
[ ] No hardcoded secrets in source code
```
