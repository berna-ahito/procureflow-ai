# ProcureFlow AI — Agent Instructions

Follow the same rules as CLAUDE.md.

Use Codex mainly as:
- second reviewer
- security reviewer
- test/debug helper

Primary builder is Claude Code.
Do not enable extra MCPs unless needed.
Do not use paid APIs.
Use `py`, not `python`, on Windows.
