---
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - app/__init__.py
  - app/main.py
  - app/core/__init__.py
  - app/core/config.py
autonomous: true
requirements_addressed:
  - NFR-CODE-1
  - NFR-CODE-3
  - NFR-CODE-4
  - NFR-SEC-1
type: execute
must_haves:
  goal: "Runnable FastAPI app scaffold with config loading and no secrets in repo"
  truths:
    - "py -m uvicorn app.main:app --reload exits 0 or starts (not import error)"
    - ".env.example exists and contains SECRET_KEY, DATABASE_URL, ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES"
    - ".env is NOT present in repo (only .env.example)"
    - "requirements.txt lists all direct dependencies with justifications in comments"
    - "app/core/config.py loads Settings from environment via pydantic-settings"
---

# Plan 01-01: Project Scaffold

## Objective

Create the project directory structure, `requirements.txt`, `.env.example`, FastAPI entry point (`app/main.py`), and settings loader (`app/core/config.py`). No models or auth yet — just a bootable app shell.

## Tasks

<task id="T01" type="execute">
  <title>Create requirements.txt with all Phase 1 dependencies</title>
  <read_first>
    - .planning/ROADMAP.md — "Packages (justified)" section under Phase 1
    - CLAUDE.md — "No random dependencies" rule
  </read_first>
  <action>
    Create requirements.txt at project root with these packages (one per line, add inline comment justifying each):
    fastapi — web framework
    uvicorn[standard] — ASGI server
    sqlalchemy — ORM (sync-first, async-optional later)
    alembic — DB migrations
    python-jose[cryptography] — JWT encode/decode
    passlib[bcrypt] — password hashing
    pydantic[email] — validation + EmailStr type
    pydantic-settings — Settings from env vars
    python-dotenv — load .env into os.environ
    httpx — test client for FastAPI TestClient
    pytest — test runner
    pytest-cov — coverage reporting
    Do NOT add any package not on this list.
  </action>
  <acceptance_criteria>
    - requirements.txt exists at project root
    - requirements.txt contains exactly these packages: fastapi, uvicorn[standard], sqlalchemy, alembic, python-jose[cryptography], passlib[bcrypt], pydantic[email], pydantic-settings, python-dotenv, httpx, pytest, pytest-cov
    - No other packages present
    - Each line has a brief inline comment (# reason)
  </acceptance_criteria>
</task>

<task id="T02" type="execute">
  <title>Create .env.example (no secrets)</title>
  <read_first>
    - docs/SECURITY_CHECKLIST.md — "No secrets committed" rule
    - .planning/ROADMAP.md — Phase 1 deliverables list
  </read_first>
  <action>
    Create .env.example at project root with these keys and example (non-secret) values:
    SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32
    ALGORITHM=HS256
    ACCESS_TOKEN_EXPIRE_MINUTES=60
    DATABASE_URL=sqlite:///./procureflow.db
    Do NOT create a .env file. Do NOT commit real secrets.
  </action>
  <acceptance_criteria>
    - .env.example exists at project root
    - .env.example contains keys: SECRET_KEY, ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES, DATABASE_URL
    - .env file does NOT exist
    - Values are obviously placeholder (not real secrets)
  </acceptance_criteria>
</task>

<task id="T03" type="execute">
  <title>Create .gitignore</title>
  <read_first>
    - .env.example — to confirm .env must be excluded
  </read_first>
  <action>
    Create .gitignore at project root. Must include at minimum:
    .env
    __pycache__/
    *.pyc
    *.pyo
    .pytest_cache/
    htmlcov/
    .coverage
    *.db
    *.sqlite3
    dist/
    build/
    *.egg-info/
  </action>
  <acceptance_criteria>
    - .gitignore exists at project root
    - .gitignore contains ".env" on its own line
    - .gitignore contains "__pycache__/"
    - .gitignore contains "*.db" to exclude SQLite files
  </acceptance_criteria>
</task>

<task id="T04" type="execute">
  <title>Create app package structure</title>
  <read_first>
    - .planning/ROADMAP.md — "Deliverables" folder tree under Phase 1
  </read_first>
  <action>
    Create these directories and empty __init__.py files:
    app/__init__.py
    app/core/__init__.py
    app/db/__init__.py
    app/schemas/__init__.py
    app/routers/__init__.py
    app/services/__init__.py
    tests/__init__.py
    All __init__.py files should be empty (0 bytes or a single blank line).
  </action>
  <acceptance_criteria>
    - Directories app/, app/core/, app/db/, app/schemas/, app/routers/, app/services/, tests/ all exist
    - Each directory contains __init__.py
    - No implementation code in __init__.py files
  </acceptance_criteria>
</task>

<task id="T05" type="execute">
  <title>Create app/core/config.py — Settings loader</title>
  <read_first>
    - .env.example — for the exact env var names to load
    - requirements.txt — confirms pydantic-settings is available
  </read_first>
  <action>
    Create app/core/config.py using pydantic-settings BaseSettings pattern.
    Class name: Settings
    Fields (with types and defaults where appropriate):
      secret_key: str (no default — required)
      algorithm: str = "HS256"
      access_token_expire_minutes: int = 60
      database_url: str = "sqlite:///./procureflow.db"
    Add model_config = SettingsConfigDict(env_file=".env") so pydantic-settings reads .env automatically.
    Export a module-level singleton: settings = Settings()
    Import: from pydantic_settings import BaseSettings, SettingsConfigDict
  </action>
  <acceptance_criteria>
    - app/core/config.py exists
    - File imports BaseSettings and SettingsConfigDict from pydantic_settings
    - Class Settings(BaseSettings) is defined
    - Fields: secret_key (str, required), algorithm (str), access_token_expire_minutes (int), database_url (str)
    - Module-level `settings = Settings()` singleton exported
    - `from app.core.config import settings` succeeds without error when .env.example values are present
  </acceptance_criteria>
</task>

<task id="T06" type="execute">
  <title>Create app/main.py — FastAPI application entry point</title>
  <read_first>
    - app/core/config.py — to import settings
    - .planning/ROADMAP.md — Phase 1 endpoint plan overview
  </read_first>
  <action>
    Create app/main.py with:
    - FastAPI app instance: app = FastAPI(title="ProcureFlow AI", version="0.1.0")
    - A health check route: GET /health returns {"status": "ok", "version": "0.1.0"}
    - Router imports are added later (stub imports commented out for now)
    - No inline business logic in main.py
    Import: from fastapi import FastAPI
    Do NOT import any routers that don't exist yet.
  </action>
  <acceptance_criteria>
    - app/main.py exists
    - FastAPI() instance created and named `app`
    - GET /health endpoint defined returning {"status": "ok", "version": "0.1.0"}
    - `py -m uvicorn app.main:app` command does not produce ImportError
    - File is under 50 lines
  </acceptance_criteria>
</task>

## Verification

```
py -m uvicorn app.main:app --host 0.0.0.0 --port 8000
curl http://localhost:8000/health
# Expected: {"status":"ok","version":"0.1.0"}
```

Also verify: no .env file created, .env.example present, requirements.txt has exactly the listed packages.
